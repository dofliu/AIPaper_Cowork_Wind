# 專案現況與交接 — 2026-08-20

**這份文件的用途**：讓任何協作者（含未來的我）不必讀對話紀錄就能接手。
對話會變長、會被壓縮、會遺失；這份文件在版本控制裡。

**新協作者請從這裡開始，讀完再去看別的文件。**

分支：**`main`**。所有工作分支（#6–#9）皆已併入並清理。
　　　**請從 `main` 取用，不要再從個別工作分支接手。**
自我測試：**14 支、421 checks**、全過（`docs/LOCAL_RUNBOOK.md` Phase 0.3 有一鍵指令）

> 這個數字是各分支合併**後實跑**得到的，不是把各分支的數字相加。
> 曾有兩個 session 同時在改 `evaluate_experiment.py` 與
> `selftest_end_to_end.py`，所以合併後一律重跑驗證，不假設互不干擾。

---

## 1. 現在卡在哪裡（一句話）

### 1.00 【2026-08-20】選擇效應現在有代數下界；同日撈到一個引用缺口

本輪有兩件事改變了論證的位置，方向相反。

**一、C2 的核心論證從觀察升級成證明。** 先前「凍結期 0.6819 是選擇效應不是停滯」
靠兩個實證（頭 18 步就 0.589、α 不變性）。現在有一個**與資料無關的代數下界**：
在已校準點的索引空間裡凍結集就是 `F = {t : S_t ≥ 6}`（等號，非近似），
於是凍結點連同它們事前看過的視窗 `N(F)` 上，超越數必然 ≥ `(6/18)·|F|`。

| α | 下界（`N(F)` 上） | 下界／α | 實測 | 實測／下界 |
|---|---|---|---|---|
| 0.001 | 0.2554 | 255× | 0.6590 | 2.58 |
| **0.01** | **0.2397** | **24×** | 0.5741 | 2.40 |
| 0.05 | 0.2505 | 5.0× | 0.6060 | 2.42 |

下界本身幾乎不隨 α 動，但它相對 α 的倍數動了 51 倍。
**同輪順帶做了前提稽核**：`frozen` 是否真的等於 6-of-18，先前只是讀程式碼相信的，
本輪對三個 α 共約 250 萬個可測試點**雙向重算，兩個方向都 0 違反**——
這是「已簽核的 6-of-18 確實被執行了」的第一次獨立驗證。

工具 `scripts/diagnose_alarm_selection_floor.py`（28 checks，含反向驗證），
輸出與完整推導見 `experiments/alarm_selection_floor_2026-08-20/`，
量測併入 `docs/FREEZE_LOCKIN_FINDINGS.md` 2.3a。
**這個界不是凍結集本身的下界，也不是分解**——兩種誤讀都會比代數多主張東西，
寫稿前務必讀該目錄 README 第二節。

**二、同一個概念在保形推論文獻裡早就有名字，而我們沒引。**
`selection-conditional coverage`／post-selection inference／**FCR 控制**
是一整支既有文獻（JRSSB 2025、CAP、arXiv:2503.16809 等）。
**這不動 R25 的定位**（我們的貢獻是這個運維機制的幾何與呈報協定，不是選擇效應這個概念），
**但它是一個真實的引用缺口**：Q1 若送到保形推論那側的審稿人手上，
一篇談「凍結期覆蓋率量不準」卻不引 FCR 的稿子第一輪就會被要求補。
細節、初步差異表與建議新增的 firewall 第六條見
`docs/literature/LITERATURE_SCAN_2026-08-20.md` 第二節。**維持 `NOVELTY_UNRESOLVED`**。

### 1.0 【2026-08-18】已提交的實驗表格還是舊協定的，頭號數字要換掉

**R24 改了尺規，但 `experiments/MD_2022_a*_evaluation/` 底下的
`comparison.md` 與 `evaluation.json` 是 2026-08-16 產生的，早於 R24。**
它們沒有 `frozen %` 與 `FAR frozen` 兩欄，頭號數字仍是混算凍結與未凍結的
pooled 值——也就是 R24 明文裁定**不得作為頭號數字**的那一個。

本輪用 main 上現在這版 `evaluate_experiment.py` 對**既有的逐案 CSV** 重算
（沒有重跑 base scorer、校準層或任何基線），結果在
`experiments/three_number_recheck_2026-08-18/`：

| α | ours 舊表（pooled，第幾名） | **ours 新表（未凍結，第幾名）** | 凍結佔比 | 凍結點誤報率 |
|---|---|---|---|---|
| 0.001 | 0.0114（第 4） | **0.0017（第 2）** | 0.5% | 0.7711 |
| **0.01** | **0.0602（第 4）** | **0.0036（第 1）** | 4.9% | 0.6819 |
| 0.05 | 0.2100（第 4） | **0.0144（第 1）** | 23.4% | 0.7068 |

**α=0.01 時，本方法從五個方法中的第四名變成第一名**——比最好的基線
（w1acas 0.0111）近 3.1 倍，比 static（0.1295）近 36 倍。

**基線的數字一個都沒動**（逐格比對到小數第十位完全相同，
見該目錄 README 第三節）。變的只有 `ours`，因為只有它有 `frozen` 欄可切。
這正是「只改呈報、不改數字」應該有的樣子——如果基線也跟著動了，
那就代表改的不只是呈報。三數字反算 pooled 的殘差：α=0.01 為 7e-18，
另兩個 α 為 0.0。

**這次重算同時獨立重現了 `FREEZE_LOCKIN_FINDINGS` 第 2.1 節**
（未凍結 0.0113 / 2,367,335 點、凍結 0.6819 / 123,005 點、整體 0.0445），
逐位相符。順帶抓到該文件兩個**標籤**錯誤（數字本身是對的）：

- 第二節標題寫 `n = 2,490,358`，但它自己表格的兩列相加是 **2,490,340**，
  差 18 —— 正好是 case 93 的裁切列數。標題那個 n 是裁切前的。
- 前言寫「誤報率只在 **50** 個正常案例上計算」，實際是 **47**
  （95 案扣掉 D1/D6 排除的四案，其中三案是正常案例）。

兩者都不影響結論，但依準則 7（統計量一律連同分母一起呈現），
**被寫錯的分母就是要更正的東西**。

**待本機處理**：`experiments/` 底下的正式輸出要由本機重跑一次完整 Phase 5
評估才能同時具備三數字誤報**與** lead time——雲端這側沒有
`event_info.csv`（原始資料不在版控裡），所以重算目錄裡的 lead-time 四欄
全部是 `n/a`。**不要拿重算目錄去覆蓋既有輸出**，那會刪掉唯一一份 lead-time
證據。詳見 `experiments/three_number_recheck_2026-08-18/README.md` 第四節。

---

**2026-08-16：CARE v6 首批真實實驗數字已產出。主要主張在初版數字上「看起來」
失敗，歸因後查明是 Freeze-on-Alert 缺少復原機制所致，核心方法本身校準正確。**

| α=0.01，正常案例 | 誤報率 | 名目 |
|---|---|---|
| **未凍結的點**（佔 95.1%） | **0.0113** | 0.01 ← 校準正確 |
| **凍結中的點**（佔 4.9%） | **0.6819** | 0.01 ← 鎖死 |
| 整體 | 0.0445 | 0.01 |

Freeze-on-Alert 沒有復原條件，形成正向回饋:誤報 → 凍結 → 參考分布過期 →
更多超越 → 告警不解除 → 持續凍結。**4.9% 的點就把整體誤報率從 0.0113
拉到 0.0445。**

消融實測（`--no-freeze-on-alert`，α=0.01，95 案）：

| | worst-bin dev | marginal dev |
|---|---|---|
| 啟用 Freeze（正式那輪） | 0.0616 | 0.0365 |
| **關閉 Freeze** | **0.0150** | **0.0077** |

關閉後 worst-bin 從第 4 名變成與 ACI（0.0165）相當、優於 DtACI（0.0198），
且為 static（0.1295）的 **8.6 倍**好。**條件化的價值是成立的。**

⚠️ **但不能就這樣關掉 Freeze。** R21 已實測:關閉後合成故障資料上的工單告警
覆蓋率從 1.000 掉到 0.000——自我遮蔽會回來。需要的是**復原機制**。

**【2026-08-16 已實測】R16 提出的復原政策不能解決這個問題。**

R16（Claude E, 2026-08-13）提過 Freeze-on-Alert Recovery Policy，數值全部沿用
已簽核參數：`K_rec = 18` 步、`D_freeze_max = 4320` 步（30 天）。本輪照該提案
實作並在 95 案真實資料上實測，結果：

| | worst-bin | marginal |
|---|---|---|
| 凍結無上限（正式那輪） | 0.0616 | 0.0365 |
| **照 R16 實作** | **0.0615** | **0.0365** |
| 完全關閉 Freeze | 0.0150 | 0.0077 |

**幾乎沒有差別。** 原因是它假設的失效型態不存在：

- `D_freeze_max` 要求**連續**凍結 30 天才觸發斷路器
- 真實資料上最長的連續凍結遠短於此，斷路器**觸發 0 次**
  （此處原寫「384 步」，數字有誤，見本節末的更正；正確值是正常案例 1,712 步、
  故障案例 2,164 步，仍全部小於 4320，所以結論不變）
- `K_rec = 18` 等價於既有 6-of-18 告警的自然解除，沒有增加任何約束

真實失效是**多次短凍結、每次超越率約 68%**，不是一次永久鎖死。R16 針對前者。

該實作已退回，未併入——一個經實測沒有作用的機制留在程式裡，只會讓下一個人
以為問題已經處理過。**這是本專案「看起來合理但是錯的」清單上該避免的模式。**

~~**下一步是設計題，需要團隊決定**~~
**【2026-08-17 已量測】那兩個方向都有上限，而且上限離名目值很遠。**

完整分析見 **`docs/FREEZE_LOCKIN_FINDINGS.md`**。三個結果改變了問題的形狀：

**一、鎖死的主導項是選擇效應，不是停滯。** 凍結內的誤報率確實隨停滯上升
（距凍結開始 0–18 步 0.5894 → 576 步以上 0.9449），但**它在第一個 18 步就已經是
0.589**——凍結還來不及讓任何東西過期。成因是結構性的：工單規則 6-of-18 的
**進入條件**就是「最近 18 點至少 6 點超越」，亦即局部超越率 ≥ 1/3。
**在一個依超越率挑出來的子母體上量超越率，量到的不會是 α。**

支持證據是 α 不變性：α 動 50 倍、凍結佔比動 47 倍，凍結內誤報率幾乎不動。

| α | 凍結佔比 | 凍結內誤報率 | 0–18 桶 |
|---|---|---|---|
| 0.001 | 0.5% | 0.7711 | 0.6225 |
| 0.01 | 4.9% | 0.6819 | 0.5894 |
| 0.05 | **23.4%** | 0.7068 | 0.6211 |

（α=0.05 有近四分之一的點處於凍結狀態。敏感度分析要呈現 α=0.05 時必須一起講。）

**二、縮短凍結的回收上限**（靜態反事實，是上限不是預測）：即使切在 36 步
＝6 小時——短於任何真實工單反應時間——整體誤報率也只從 0.0445 降到 **0.0275**，
仍是地板 0.0113 的 2.4 倍。多數凍結本來就短（p50 = 22 步），而短凍結內的誤報率
已經是 0.59。

**三、六種吸收政策在合成資料上實測，沒有一個同時通過兩個 fixture。**
`freeze` / `bin_local` / `gated` / `winsor_alpha` 都在良性水準位移上鎖死；
`none` 與 `winsor_max` 都在慢速故障上自我遮蔽。**「慢故障」與「良性位移」兩欄
的值六個政策完全相同**——在分數串流本身之內，這兩者是同一個訊號。
要區分它們，資訊必須來自分數以外（見 FINDINGS 第 4.3 節：工單結案）。

**因此改的是呈報方式，不是機制。**
**【2026-08-17 已裁決並實作】劉老師裁定誤報率改為三數字呈報（R24）。**
評估尺規（`evaluate_experiment.py`、`regime_conditional_calibration.py`）已改為
未凍結最差分箱偏差／凍結佔比／凍結點誤報率三者並列，pooled 仍記錄且可反算，
以自我測試釘住（見 6.5）。已簽核的模型參數完全未動。

**另**：本文件先前寫「最長連續凍結 384 步」是錯的，該數字出自 R16 那輪被其作者
標為不可信的儀器。從 `frozen` 欄重數，正常案例最長 **1,712 步（11.9 天）**，
故障案例 2,164 步。R16 的結論不變（4320 仍大於所有 α 下的最長 run，觸發 0 次），
但引用的證據數字已更正。



**2026-08-15：Base Scorer 1（MD_2022）的 C0–C6 在真實資料上三個風場全部
`gate_status=PASS`。前置工作結束，下一步是 Phase 5 產出實驗數字。**

| 風場 | 案數 | C0–C6 |
|---|---|---|
| Wind Farm A | 22 | **PASS** |
| Wind Farm B | 15 | **PASS** |
| Wind Farm C | 58 | **PASS** |

六趟評分器（A/B/C × run1/run2）已完成，per-farm 證據齊備，
`bit_identical` 決定性通過。

**但這不等於可以在稿件寫「C0–C6 通過」**，有兩個限制：

1. C0–C6 的**定義本身仍未被批准**，程式輸出仍標 `gate_definitions_ratified: false`。
   今天新增的「評分器未評分的列視為非 evaluable」是對該定義的第一次修改
   （見 6.1 下方），需要一併正式追認。
2. **D5 要求主張在兩個 scorer 上都成立**，Base Scorer 2 尚未實作（見 6.2）。
   目前通過的只有其中一個。

~~真正的科學風險在第 6 節的非劣性未通過。~~
**2026-08-15 更新：該項已解除**（見 6.1）。目前沒有已知的科學風險擋在前面。

---

## 2. 已經確立的資料事實（必須寫進手稿）

這些是**跑真實資料才發現的**，不是從文件推得的，且都影響論文怎麼描述資料。

### 2.1 CARE v6 的功率是標么值，不是 kW

三個風場的 `active_power` 值域都是 `[0, ~1.0]`，但字典宣告 `kW`。

| 風場 | p01 | p50 | p99 |
|---|---|---|---|
| A | −0.009 | 0.113 | 0.976 |
| B | −0.004 | 0.276 | 1.029 |
| C | −0.004 | 0.230 | 1.012 |

方法不受影響（標么值仍是有效特徵，且三場一致），但**手稿不能寫 kW**。
signal map 的宣告單位已改為 `p.u.`。

### 2.2 Farm A 沒有主軸承通道

字典裡只有齒輪箱高速軸與發電機 DE/NDE 軸承——**不同元件**。
以 `not_available` 明確宣告（C0 接受宣告，靜默缺席會 FAIL）。
劉老師 2026-08-15 追認。

**這是論文限制章節的依據**：Farm A 的主軸承故障對 MD_2022 不可見。
`selftest_md2022.py` T3 專門釘住這件事——如果哪天它開始偵測得到，
代表限制敘述過期了，測試會失敗。

### 2.3 Farm A 的 `active_power` 一度抓成「可能發電量」

字典裡有兩個 kW 通道：

```
power_29   "Possible grid active power"   ← IEC 61400-26 的可用率術語，模型值
power_30   "Grid power"                    ← 實測
```

「Possible power」是「這台機在當下風速*本來能*發多少」，由風速和功率曲線算出，
**一台正在衰退的機組在它上面完全不留痕跡**——那正是我們要偵測的東西。

已改為實測通道優先；只剩能力值通道時拒絕解析並說明原因。
現用 `power_30_avg`。

### 2.4 主軸承通道有故障碼 — **Farm C 與 Farm B 都有**

Farm C 的 `sensor_194/195` 有超過 1% 的列固定是 850.0。與另外三個約 46 °C 的
通道平均後，主軸承溫度變成 **363 °C**。餵進 Mahalanobis 會產生巨大距離＝假警報，
且不報錯。

已在**逐通道**層級過濾（取平均之前），過濾政策與拒絕筆數記在
`scorer_summary_<farm>.json` 的 `range_filter`。修正後 p99 = 53.957。

**五個通道都是真的主軸承**，不要剔除：乾淨資料上跨組相關 0.945–0.948，
中位數 50.7 vs 46（內圈比外殼量測熱，物理正確），都遠高於環境溫度 28 °C。

**【2026-08-15 六趟評分器實跑後補充】這不是 Farm C 獨有的。**
Farm B 的主軸承對 `sensor_51/52`（signal map 取兩者平均）同樣有故障碼：

| 風場 | 通道 | 拒絕筆數 | 佔該場總列數 |
|---|---|---|---|
| B | `sensor_52_avg` | 4,012 | 0.47% |
| B | `sensor_51_avg` | 464 | 0.05% |
| C | `sensor_194_avg` | 67,896 | 2.13% |
| C | `sensor_195_avg` | 67,871 | 2.13% |

**兩場的失效型態不同，這點值得寫進手稿**：
Farm C 兩個通道幾乎同時失效（67,896 對 67,871，差 25 筆），像是共用同一條
擷取路徑；Farm B 的兩個通道**各自獨立失效**（4,012 對 464，差 8.6 倍）。

由此帶出一個必須揭露的細節：逐通道過濾是在取平均**之前**做的，所以當
`sensor_52` 被拒而 `sensor_51` 沒有時，那些列的 `main_bearing_temperature`
**是單一通道值，不是兩通道平均**。這是刻意的設計（一個壞通道不該連累另一個），
但它意味著那些列的特徵雜訊特性與其他列不同。Farm B 約 0.47% 的列屬於此類，
量級上可忽略，**但手稿不能寫成「一律取兩通道平均」**。

Farm A 沒有主軸承通道（2.2），而那正是三場中唯一帶故障碼的訊號——
所以 Farm A 的拒絕數為 0 是合理的，不是濾網沒作用
（已確認 `range_filter.enabled = true` 且五個範圍都載入）。

### 2.5 Farm C 的 `sensor_146/147` 已排除

四個候選轉速通道：

```
sensor_144/145  "Rotor speed 1/2"                      p50=9.79   彼此 r=1.0000
sensor_146/147  "Rotor speed gearbox main shaft 1/2"   p50=80     彼此 r=0.2073，會出現負值
```

146/147 名義上量同一根軸卻互不相關，量級也不對。四個一起平均會把中位數
從 9.8 拉到 46.6。以 `--exclude-sensor` 排除，排除紀錄與描述寫在
`signal_map_report_Wind_Farm_C.json` 的 `excluded_by_operator`。

### 2.6 Farm A/B 字典的度數符號在檔案裡就已損毀

存的是 U+FFFD 的 UTF-8 位元組。utf-8 讀會得到 U+FFFD，cp1252 讀會得到字面
`ï¿½`。原字元救不回來，所以標成 `UNREADABLE_IN_SOURCE`，由 `--unit-override` 宣告。

### 2.7 其他

- CSV 分隔符號是 `;`，不是 `,`。
- 三個風場的欄名不同：`wind_speed_3_avg` / `wind_speed_61_avg` / `wind_speed_236_avg`。
  評分器輸出時統一正規化成 `timestamp` / `wind_speed` / `anomaly_score` + `signal_*`。
- 溫度 A/B 是 `degC`、C 是 `Celsius`；轉速 A/B 是 `rpm`、C 是 `1/min`。
  字面不同、實質相同，已用數值範圍確認。
- Farm C 轉速停機時讀到 −5.5，是感測器偏移不是故障碼，範圍下限設 −6.0 容納。

---

## 3. 已簽核的參數（未經劉老師同意不得更動）

2026-08-11 簽核：

| 項目 | 值 |
|---|---|
| α | 0.01（主要）、0.05、0.001 |
| 工單告警規則 | 6-of-18 |
| 視窗 W | 1440 步 |
| 風速分箱 | <4 / 4–8 / 8–12 / ≥12 m/s |
| 每格最少樣本 | 500 |
| 非劣性邊際 | lead-time 損失 ≤ 2 天 |
| Freeze-on-Alert | 啟用 |

2026-08-15 追認：

- C1 non-evaluable 門檻 30% → **5%**（依據：G4 深掃 15 案，中位數 0.0037、
  p95 0.0237、**最大 0.0364**——是證據不是慣例）
- D5 範圍限縮至 Farm B/C
- D1/D6 排除 case 32/56/72/87，case 93 裁切 → 實驗用 **91 案**（非 95）
  · 裁切點 `2023-08-24T13:00:00`（case 33 的評估視窗開啟時刻，同為 Farm C
    機組 43，兩案標籤相反、重疊 0.12 天）
  · **2026-08-16：裁切先前只是設定檔註解，沒有任何程式套用**。已實作為
    `evaluate_experiment.py --trim-case`，由 `run_pipeline.py` 的
    `experiment.trim_cases` 自動帶入，結果記在 `evaluation.json` 的
    `trimmed_cases`
- 多感測器取平均
- W1-ACAS `--max-past` = 1440（對齊 W）
- Farm C 用 `power_6`、`wind_speed_236`

2026-08-20 裁決（R26 pre-run contract 與寫作界線；**不動任何模型參數**）：

- **POGO 的 group 數：`k = 4` 與 `k = 5` 都跑。** 本研究已簽核的 `k = 4`
  group 定義**不動**；`k = 5`（作者 script 在四個 subgroup 前加一個 all-ones
  marginal group）記為 **POGO 那一側的執行參數**，即「照作者預設跑」。
  **POGO 的頭號數字取兩者中較好的那一個。**
  · 為什麼不是選擇性呈報：本方法**沒有任何可挑的自由設定**，
    所以這個不對稱**只往對本方法不利的方向倒**。
  · **這個裁決在看到任何結果之前做成，符合 G7。**
  · 後補證據：全文確認 `k` 只以 `ln(k)` 進入 Theorem 4.1 的 `U_T(k)`，
    在本專案尺度上 `k=4` 與 `k=5` 的界只差 **0.66%**——
    **`k` 不是理論驅動的選擇，是經驗問題**，正好就是「兩個都跑」的理由。
- **POGO 的 `binary_groups = True`。** 理由不是「作者預設如此」，而是
  **本研究的 group 本來就是硬性互斥的 one-hot**；設 `False` 等於宣告
  我們的 group 是軟的——那是假的。**符合資料實情的設定，不是調參。**
- **claim firewall 新增第六條**：禁止任何形式的「我們發現／首次指出選擇效應
  會使條件覆蓋率主張失真」。選擇後推論（POSI）、`selection-conditional
  coverage` 與 FCR 控制是既有文獻。**這一條不改 R25 的定位**，只加一條禁止；
  同時它帶來一項**引用義務**（見 6.9）。

2026-08-17 追認（R24，評估協定，不動模型參數）：

- **誤報率改為三數字呈報**（未凍結最差分箱偏差／凍結佔比／凍結點誤報率），
  不再以單一 pooled 值當頭號數字。理由與護欄見 6.5，量測見
  `docs/FREEZE_LOCKIN_FINDINGS.md`。這是**尺規改動**，α、6-of-18、W、
  Freeze-on-Alert 等已簽核參數一律不變。

---

## 4. 工具清單（34 支腳本）

### 產生數字的

| 檔案 | 做什麼 |
|---|---|
| `regime_conditional_calibration.py` | **本論文的提出方法**：分風速區間的線上保形校正 |
| `base_scorer_md2022.py` | Base Scorer 1（Mahalanobis），md2022-v1.2 |
| `baseline_w1_acas.py` | W1-ACAS 重實作，`is_author_code: false` |
| `baselines_online_calibration.py` | static split conformal / ACI / DtACI |
| `evaluate_experiment.py` | 共同評估尺規（所有方法一律 6-of-18） |
| `run_pipeline.py` | 設定檔驅動的單一指令執行器 |

### 資料準備與閘門

| 檔案 | 做什麼 |
|---|---|
| `care_v6_manifest.py` | D0 manifest（已關閉） |
| `care_v6_signal_map_builder.py` | 從 `feature_description.csv` 建 C0 signal map |
| `base_scorer_compatibility_check.py` | C0–C6 gate，c0c6-gate-v2.0 |
| `care_v6_split_audit.py` | D1/D6 洩漏稽核 |
| `care_v6_quality_scan.py` | G4 深掃 |
| `physical_ranges.py` | **物理範圍的唯一定義**，評分器與檢查器共用 |

### 診斷（本輪新增，都是為了回答「這個數字為什麼怪」）

| 檔案 | 做什麼 |
|---|---|
| `check_unit_consistency.py` | Phase 5.1，跨風場單位一致性，exit 0/1/2 |
| `inspect_channels.py` | 逐通道百分位＋sentinel 計數＋兩兩相關 |
| `check_power_channel.py` | 分辨「實測功率」與「可能發電量」 |
| `sensor_identification_profile.py` | 統計式感測器辨識（已次於字典） |
| `diagnose_earliness_gap.py` | 提前量缺口三假說歸因＋偵測門檻 H 敏感度掃描（見 6.1） |
| `diagnose_freeze_lockin.py` | 凍結鎖死的幾何：run 長度、連帶凍結、停滯剖面、有界凍結上限。讀既有輸出，不重跑 |
| `diagnose_alarm_selection_floor.py` | **2026-08-20 新增**。6-of-18 強制的超越率代數下界＋`frozen` 是否真的等於該規則的雙向前提稽核。讀既有輸出，不重跑 |

---

## 5. 這個專案最重要的一課

**到目前為止最嚴重的缺陷，沒有一個會報錯。**

它們全部照常跑完、照常輸出漂亮的數字，只是數字是錯的。而且**全部都是拿真實
資料實跑才撞到的，沒有一個是讀程式碼讀出來的**。

清單（給後續協作者當檢查清單用）：

| 缺陷 | 後果 |
|---|---|
| FAR 算在故障案例上 | 把真陽性當誤報，讓最好的偵測器看起來最差（0.3102） |
| score CSV 表頭 `wind_speed` 重複 | DictReader 只留後者；感測器掉線的列風速被讀成空 → 落錯分箱 → **偏誤核心主張** |
| `[FARM:]` 前綴用子字串比對 | `A:` 命中三個風場（`farm` 裡有 a），兩個風場用錯誤風速欄分箱 |
| 把「可能發電量」當實測功率 | 最有資訊量的特徵失去偵測衰退的能力 |
| 850.0 故障碼進特徵向量 | 巨大 Mahalanobis 距離＝假警報 |
| `scorer_summary.json` 固定檔名 | 三風場寫同一目錄，Farm C 蓋掉 A 和 B |
| 單位檢查器用比例比較攝氏 | 三個假警報。**會叫錯狼的檢查器比沒有更糟**——它訓練人忽略它 |
| 診斷工具預設只讀 8/58 個檔案 | 印「from 8 case file(s)」卻不說總數，把一角當全貌 |
| `median_lead_days` 只在有偵測到的案例上取中位數 | 漏檢離開母體而非計為漏檢 → **漏掉越多困難案例分數越好看**。dtaci 的「提前 33.44 天」是 6 取 1 |
| lead time 無下界 | 故障發生**之前**的誤報被計為提前預警，而且報得越早收割越多 |
| D1/D6 的 case 93 裁切只寫在設定檔註解裡 | 沒有任何程式套用它，也不會報錯——重疊視窗會照常進入正常案例的誤報統計 |
| 裁切用字串比較時間戳，資料寫 `2023-08-24 13:00:00`、參數寫 `...T13:00:00` | 空格的 ASCII 小於 `T`，所以每一列都被判在裁切點之前，**裁切靜默什麼都沒做**。上一項的同型缺陷，第二次出現（2026-08-17，`diagnose_freeze_lockin.py`）。修法不只是比對正規化，還要**輸出實際丟棄的列數** |
| 一個被作者標為「不可信」的數字仍流進現況文件 | R16 那輪的凍結長度儀器只記錄經由新路徑結束的 run，commit 訊息已聲明不可信，「最長 384 步」還是被寫進第 1 節並被後續引用。實際是 1,712 步 |

最後兩項合起來，讓一個評估器缺陷被誤判為方法缺陷，並以「主要限制條件」
呈報上去，**差一步就會導致動用已簽核參數去修一個量尺的錯**（見 6.1）。

**由此形成的工作準則**：

1. 每支工具都要有自己的行為測試。
2. 新測試要**反向驗證**——把修正還原，確認測試真的會失敗。只會 PASS 的測試是裝飾。
3. 檢查要能**兩個方向都失敗**。只往一邊失敗的檢查不是證據。
4. 取樣要說明涵蓋率。跨距取樣可能完整漏掉週期性異常（實際發生過：
   每 40 列注入、每 20 列取樣，完美錯開）。
5. 物理範圍、閘門門檻這類定義只能有**一份**。兩支工具對「什麼叫可能」意見不合，
   比其中一支寫錯更糟。
6. **任何要呈報為「方法限制」的負面結果，呈報前先在真值已知的合成資料上做一次
   歸因**，確認那個限制不是量尺造成的。`scripts/diagnose_earliness_gap.py`
   是這個流程的範例：先逐一重現被質疑的數字，再逐項排除候選解釋。
7. **統計量一律連同分母一起呈現。** 中位數、比率、平均，只要母體會隨方法變動，
   就必須把 n 印在旁邊。上表最後兩項都是分母被藏起來造成的。

---

## 6. 未解決的事（依重要性排序）

### 6.1 ~~【最重要】非劣性未通過~~ → **已解除（2026-08-15, R22）**

**這一項先前是本文件的頭號風險，現已查明為評估指標的缺陷，不是方法的缺陷。**
`min_bin_samples` **維持 500，不需要重啟**。

原本的呈報是：本方法相對 static 損失約 8 天 lead time、超過 2 天邊際，
並詢問是否開放調整 `min_bin_samples`。`scripts/diagnose_earliness_gap.py`
把那組數字逐一重現後歸因，三個候選解釋兩個被排除：

| 假說 | 結論 | 依據 |
|---|---|---|
| 暖機（每箱緩衝填得慢） | **排除** | 首個可判定索引 530 對 pooled 500，僅差 0.2 天；故障最早在索引 3800 才爬升 |
| 工作點不對等 | **排除** | 各方法邊際誤報率都在名目 0.05 附近（0.045–0.059） |
| **故障前誤報被計為提前預警** | **成立，且是全部缺口** | static 的 16.53 天當中有 **6.11 天**來自故障開始前 880 步的告警；aci 為 8.28/18.70。本方法 0.00 天 |

兩者回報的數字都超過該 fixture 物理上可能的 10.42 天上限——
它們在「偵測」一個當時還不存在的故障。

**eval-v1.0 的兩個缺陷（都不報錯）已修正為 eval-v1.1：**

1. `median_lead_days` 只在有偵測到的案例上取中位數，漏檢直接離開母體
   → 漏掉越多困難案例分數越好看。dtaci 的「提前 33.44 天」是 6 取 1，
   w1acas 也是 6 取 1，本方法是 6 取 6，舊表格看不出來。
   現在一律附 `detection_rate` 與分母，並新增免參數的
   `median_lead_days_missed_as_zero`。
2. lead time 無下界 → 新增 `--detection-horizon-days`，**刻意不給預設值**。

**修正後，本方法在所有掃描門檻下非劣性皆通過**（12 案 fixture、對 static、
邊際 2 天）：未設限 −0.12／H=3 0.00／H=5 0.00／H=7 −0.40／H=10 −0.95／
H=14 −0.64（負值代表本方法反而更早）。偵測率 ours 6/6、static 6/6、
aci 4/6（設限後 1/6）、dtaci 1/6（設限後 0/6）、w1acas 1/6。

條件覆蓋率的主張不受影響，依然成立（最差分箱偏差 0.0061 對基線 0.1611–0.2387）。

**仍待劉老師裁決**：偵測門檻 H 是否納入評估協定、取值多少。這是**新增的評估
參數**，程式不預設。掃描實據見《[裁決請求] 2026-08-15 R22》：H 太小會截斷真實
提前量（H=3 時所有方法都被壓成 3.00），H 超過物理上限則故障前誤報又被放回來
（H=14 時 aci 從 1/6 回到 2/6），可用區間在兩者之間。

> 仍是合成資料。它證明的是「指標現在量到它名稱所指的東西」，
> **不是** CARE v6 的實驗結果。真實數字仍需本機的 score stream。

### 6.2 Base Scorer 2（MainBearing_2026）

需要主軸承 SCADA 框架論文（AME 2026）的實作，雲端這側沒有，只能由本機提供。
D5 要求主張在**兩個** scorer 上都成立，所以這是硬需求。

### 6.3 CARE adaptive threshold 基線

定義在尚未取得的 CARE 論文中。目前明確標記 `NOT_IMPLEMENTED`，呼叫即拋例外。
**寧可缺一個基線，不可拿近似值冒充原作。**

> 這是最適合分派出去的工作：純文獻、與程式碼無關、做完直接解鎖一個基線、
> 對錯容易驗證（定義要能逐字對照原文）。

**2026-08-16 進展：文獻已定位，全文仍未取得。**
`docs/literature/CARE_PAPER_ACQUISITION.md` 記下了期刊版與預印本的完整書目、
資料集 Zenodo DOI，以及拿到 PDF 後要逐項回答的**八個核對問題**。

雲端這側取不到全文——網路政策封鎖 arxiv／mdpi／zenodo／publica.fraunhofer／
openalex／semanticscholar，實測全部 403，不是暫時性錯誤。**本機端下載約三分鐘**
（MDPI *Data* 為 open access），步驟寫在該文件第四節。

同一份文件也解掉了 R10 遺留的「CARE v6 DOI 疑義」的一半：資料集 DOI 為
`10.5281/zenodo.14006163`（CC BY-SA 4.0），另有疑似較早版本的記錄 `10958775`。
論文 44/51 對 v6 metadata 45/50 的差異出自哪一次改版，要在下載時一併確認。

> **核對問題 Q6 可能會讓這一項直接消失**：需先確認 CARE 論文裡的
> "adaptive threshold" 究竟是一個**基線方法**，還是資料集**標註時用的工具**。
> 若是後者，6.3 的正確處置是**撤銷這個基線**，不是實作它。
> 目前二手摘要對這一點的描述最不可靠（把 SCADA 說成聲射訊號，疑似混入他篇）。

**【2026-08-18】Q6 有了二手答案，而且方向與上面的猜測相反。**

本輪掃描取得的描述（**二手，疑似轉引自論文正文，未經核對**）指出：
adaptive threshold 用於 **Wind Farm A 與 B**，作法是以神經網路回歸模型學習
「autoencoder 輸入 → 重建誤差 L2 範數」的映射，預測時比較**預期**重建誤差與
**實際**重建誤差。若屬實：

1. 它是**方法元件**，不是標註工具 → 「撤銷這個基線」的路徑不成立，6.3 要留著。
2. 它**只用在 Farm A/B**，而本專案 D5 範圍已限縮至 Farm B/C
   → **兩者只在 Farm B 重疊**，基線能比的風場只有一個。
3. **它綁在 autoencoder 的重建誤差上**，不是可套在任意分數串流上的門檻政策。
   本專案的凍結分數是 Mahalanobis 距離。所以「把 CARE 的 adaptive threshold
   套到我們的 base scorer 上」在定義上就不是原作做的事。

**第 3 點把 6.3 從工程題變成裁決題**：是連 autoencoder 一起重現
（等於再加一個 base scorer 的工作量），還是明確聲明本論文不含 CARE 基線
並在 Limitations 說明原因？**建議後者**，理由是前者會讓本論文多背一個
自己實作的偵測器，而本論文的定位是「套在既有凍結偵測器上的校準層」。
無論選哪一個，都要先拿到全文核對上面那段描述。
完整記錄見 `docs/literature/LITERATURE_SCAN_2026-08-18.md` 第六節。

### 6.4 待追認

- Farm C 850.0 過濾政策（2.4）
- Farm C `sensor_146/147` 排除（2.5）
- 方法論筆記 v4 是否正式取代 v3
- **偵測門檻 H 是否納入評估協定、取值多少**（6.1；程式刻意不預設，掃描實據已備）
- ~~誤報率呈報方式~~ **✅ 已裁決 2026-08-17（R24，改為三數字呈報，見 6.5）**
- 【2026-08-16 新增】新穎性語彙限制增列第四條：
  `conditional conformal prediction for wind turbines` 不得單獨作為主詞，
  必須寫明條件化的對象（見 **6.6**）
  → **2026-08-18 的 R25 裁決已訂下更嚴格的 claim firewall**（見 6.7），
  這一條被涵蓋在內。仍列在此處是因為它本身**尚未被個別追認**，
  而 R25 firewall 管的是演算法新穎性主張，兩者範圍不完全相同。
- ~~【2026-08-18】R25 貢獻定位~~
  **✅ 已裁決 2026-08-18 21:52（改為 protocol-and-evidence，見 6.7）**
- 【2026-08-19 新增】`docs/method/POGO_COMPATIBILITY_GATE.md` 的執行 owner
  尚未指派（6.8）。這不是裁決題，是排程題，但沒人認領就不會動。
- ~~【2026-08-20】POGO 的兩項 pre-run contract~~
  **✅ 已裁決 2026-08-20（見第 3 節與 gate 3.5）**
- ~~【2026-08-20】claim firewall 第六條~~
  **✅ 已裁決 2026-08-20（見第 3 節與 `docs/manuscript/README.md` 界線四）**
- **【2026-08-20 新增，取代上面兩項成為 R26 的下一個關卡】G3 狀態契約**
  ——現在唯一還可能判 `NOT_COMPARABLE` 的一關，三項都必須在跑之前寫死：
  wealth process 跨 case 攜帶與否、凍結期間是否跳過 `update`、warm-up 如何對齊。
  **這不是裁決題也不是排程題，是規格題**：留給實作者臨場決定，
  等於讓實作決定結論。詳見 6.8 與 gate 第 6 節。

### 6.5 【2026-08-17 已裁決並實作】誤報率改為三數字呈報（R24）

依 `docs/FREEZE_LOCKIN_FINDINGS.md` 的量測，**不再**用單一 pooled 誤報率，
劉老師裁定改為三個數字一起報。已落實到評估尺規：

| 報什麼 | α=0.01 實測（2026-08-16 那輪重讀） | 意義 |
|---|---|---|
| 未凍結點的最差分箱偏差 | 0.0036（case-mean）／0.0016（point-pooled） | 條件覆蓋率主張本體 |
| 未凍結點的誤報率 | 0.0113 | 名目 0.01，校準正確 |
| **凍結佔比** | 4.9% | 方法的代價，必須以第一級數字呈現 |
| 凍結點的誤報率 | 0.6819 | 讓讀者能自行還原 pooled（0.0445） |

> 先前本節把「未凍結最差分箱偏差」與「未凍結誤報率」混寫成同一個 0.0113。
> 兩者是不同量：0.0113 是誤報率（FAR），最差分箱偏差是 0.0036。R24 實作後
> 兩者分欄輸出，不再混淆。

理由：凍結期間方法**刻意暫停校準**以保住偵測；在暫停校準的期間報一個校準保證，
量到的是警報政策，不是校準層。防止這變成自利定義的護欄是凍結佔比——
它必須攤在明面上，讀者能自己算回 0.0445。

**已實作（評估協定改動，不動模型參數）：**

- `regime_conditional_calibration.py` 的 `per_bin_false_alarm_rates` 新增
  `three_number_report`：依 `frozen` 欄把校準點切成凍結／未凍結兩群，
  分別給偏差與誤報率，並用三數字反算 pooled、記錄殘差
  （`pooled_reconstruction.exhaustive` 實測 `True`，殘差 ~7e-18）。
- `evaluate_experiment.py` 的 `comparison.md` 表頭改為
  `worst-bin dev (unfrozen) | frozen % | FAR frozen | pooled dev | …`，
  三欄相鄰、一律並列（把凍結佔比放遠等同於藏起分母，是本協定要防的事）。
  沒有凍結機制的基線該欄印 `—` 而非 `0%`——結構性的沒有，不是量出來的零。
  新增 `--freeze-state-column`（預設 `frozen`）。
  `evaluation.json` 記 `false_alarm_protocol`（版本 `three-number-far-v1.0`）。
- 釘樁：`selftest_regime_conditional.py` T7（13 checks，含建構型 fixture——
  忽略凍結切分就會把 0.05 報成 0.109，任一斷言都會抓到）、
  `selftest_end_to_end.py` T6（13 checks，跨整條管線 + 渲染表格 + 反算恆等式）。

**手稿**：Results 與 Limitations 現在可以定稿了——連同停滯剖面與 α 不變性
兩張表一起寫（見 FINDINGS 2.2、2.3）。審稿人會盯凍結佔比這一點。

同時待決（與 R24 分開，仍未裁決）：
- `regime_conditional_calibration.py --absorption` 的五個消融是否保留在版控中
  （預設不啟用、有測試釘住；價值是讓下一個人不必重做這四次否證）
- 工單結案作為凍結解除訊號（FINDINGS 4.3）是否值得投入——
  前置問題是「CARE v6 上能不能不洩漏地評估」，目前無解

### 6.6 新穎性紅旗 — 一篇 2026-05 新文獻待全文核對

> **2026-08-17 合併時改號**：本節原為 6.5（PR #13），與同日裁決的 R24 誤報率
> 呈報（現 6.5，PR #14）撞號。兩份 PR 各自新增了一個 6.5，合併後會出現兩個
> 同號小節，且 6.4 的「見 6.5」會指向錯的那一個。本節改為 6.6，交叉引用一併更正。
> **這正是 8.1 記載的撞號問題，只是這次發生在版控裡而不是 Drive 上。**

`Temporal Conformal Prediction for Wind Turbine Anomaly Detection:
A Robust Distribution-Free Framework with Conditional Clustering`（2026-05-28）
同時佔用 conformal／wind turbine／**conditional** 三個詞。

初步差異看起來成立——**它條件化的是「哪一群機組」，本論文條件化的是
「當下處於哪個運轉區間」**，且它自建偵測器而我們套在凍結分數之上——但這是
二手摘要的分析，**不是裁決**。依 R17 程序須先取得全文、填完 Mandatory
Overlap Check 四欄才能結案。詳見 `docs/literature/NOVELTY_WATCH_2026-08-16.md`。

**在此之前 Related Work 不應動筆。**

**【2026-08-18】本節狀態，Drive 與 GitHub 對不上。** Drive 索引 v1.5
在【已結案】欄寫「R23 已由 Gemini Spark 完成 Mandatory Overlap Check 四欄，
標記 NOVELTY_RESOLVED」，卻又在【尚未裁決】欄寫「6.6 需取得全文才能結案」
——同一份文件，兩種說法。R17 程序要求四欄以**全文**填寫，全文未取得就填不完，
所以合理的解釋是填的是「以二手摘要為據的初步四欄」，而索引轉述時把
「初步」丟掉了。**本輪不裁決，維持較保守的一邊**：本節維持
`NOVELTY_UNRESOLVED`，Related Work 仍不動筆。請 Gemini Spark 確認該四欄
是依全文還是依摘要填的。

### 6.7 【2026-08-18 新增・比 6.6 更優先】方法層語彙被佔用

例行掃描撈到：

```
Parameter-Free and Group Conditional Online Conformal Prediction
arXiv:2606.00419（2026-06）
```

**6.6 擋住的是 Related Work，這一篇可能擋住 contribution statement。**
6.6 那篇佔的是應用層語彙（conformal + wind turbine + conditional），
差異寫得出來；這一篇佔的是**方法層**——本方法用一句話講就是
「線上保形校準，覆蓋率按分組條件化」，而那正是它的標題。

初步差異（**二手，不是裁決**）：它是一般設定下的演算法與覆蓋保證，
本論文是**物理定義的運轉區間**、套在**既有凍結分數**之上、貢獻型態是
**協定與實證**（最差分箱偏差＋三數字誤報＋工單規則下的非劣性），
並涉及 Freeze-on-Alert 的鎖死幾何——後者目前看起來沒有人做過。

**若差異成立，本論文的貢獻應重新定位為「協定與實證」而非「演算法」。**
這會動到 abstract 的第一句，不只是一段 related work。

~~**建議列為 R25 裁決請求**~~
**【2026-08-18 已結案並裁決】重疊成立，貢獻定位已改。**

依 R17 程序取得 arXiv:2606.00419**v4** 全文（另一 session 於 2026-08-18 21:14
完成，雲端這側的 arXiv 封鎖至 2026-08-19 仍在，見
`docs/method/POGO_COMPATIBILITY_GATE.md` 第 3 節的實測），填完 Mandatory
Overlap Check 四欄，劉老師於 **2026-08-18 21:52 正式裁決**：

> 批准 R25 改定位，並建立 POGO compatibility gate。

**R25 狀態：`OVERLAP_CHECK_COMPLETED / CONTRIBUTION_REPOSITIONING_APPROVED`。**

**新的貢獻定位（這是本專案主張邊界的變更，不是措辭調整）：**

| | 舊 | 新 |
|---|---|---|
| 貢獻型態 | 演算法 | **wind-turbine O&M protocol-and-evidence contribution** |
| abstract 第一句落在 | 「我們提出一個 group-conditional 的線上保形校準器」 | 三項協定與實證（見下） |

可守的三項主張是 6.5 的三數字協定、`FREEZE_LOCKIN_FINDINGS` 的鎖死幾何
（選擇效應）、以及六個吸收政策的否證——都是「協定與實證」而非「演算法」。

**Claim firewall（禁止寫入稿件，已寫進 `docs/manuscript/README.md` 界線四）：**
first／new group-conditional OCP、parameter-free 最佳化、任何 group-conditional
coverage guarantee、`regime-aware`／`regime-dependent` 作為主詞、
以及任何形式的「我們優於／不需要 POGO」（gate 未跑，**反向結論同樣未經檢查**）。

**貢獻句已解凍**（裁決明文允許在 firewall 下起草），
初稿見 `docs/manuscript/00_contribution_statement.md`（2026-08-19 交付）。
**Related Work 仍凍結**（6.6 未結案 + POGO gate 未執行）。

同輪另有兩項只需監看、不需裁決：arXiv 2606.20115（開場論證撞車）與一篇
digital twin 用水預測研究使用 `regime-aware calibration` 一詞（詞彙撞車）。
兩者都支持**維持** `operating-regime-conditional` 這個主詞、不要簡寫。
**2026-08-19 掃描再添兩筆 `regime-*` 用法**（CALIBURN 的
`regime-dependent conformal risk control`、VaR 的 `regime-weighted conformal
calibration`），三筆獨立用法使這條規範從建議升級為禁止項，
見 `docs/literature/LITERATURE_SCAN_2026-08-19.md` 第二節。

### 6.9 【2026-08-20 已裁決】FCR／selection-conditional coverage 的引用義務

`LITERATURE_SCAN_2026-08-20.md` F10：選擇後推論（POSI）、
`selection-conditional coverage` 與 **FCR 控制**是既有文獻
（JRSSB 87(4):1239 2025、CAP arXiv:2403.07728、arXiv:2503.16809 等）。

**這不動 R25 的定位**——可守的是本運維機制的選擇幾何、其代數下界（1.00）
與三數字呈報協定，不是選擇效應這個概念。已裁決新增 claim firewall 第六條。

**但它同時是一項引用義務**：談「凍結期覆蓋率量不準」而不引 FCR，
Q1 若送到保形推論那側的審稿人手上，第一輪就會被要求補。

**另需寫一段答辯**（Discussion 或 Limitations，見 `docs/manuscript/README.md`）：
FCR 控制是「在被選中的單位上把保證救回來」；本論文**刻意不救**——
凍結期間暫停校準正是 Freeze-on-Alert 存在的目的（R21：關閉凍結後合成故障的
工單覆蓋率 1.000 → 0.000）。要在凍結點恢復覆蓋率，等於要求校準器在凍結期間
繼續適應，那會把凍結機制拆掉。三數字協定是**呈報側**的對應物，不是控制側的。

**待全文核對後才能定稿**（arXiv 雲端封鎖，已列入本機下載清單第 7 節）。
維持 `NOVELTY_UNRESOLVED`。

### 6.8 【2026-08-18 建立】R26 — POGO 相容性 gate（尚未執行）

R25 裁決的第二半：在把 POGO 當成基線比較**之前**，先證明兩個方法的輸入語義、
group 定義、時間狀態、輸出介面與 O&M 決策語義是同義的。
**規格正本在 Drive（R26 v1.0），版控版本與可執行部分在
`docs/method/POGO_COMPATIBILITY_GATE.md`。**

| Gate | 狀態（2026-08-20，當日兩次更新後） |
|---|---|
| G0 source/version receipt | **`SOURCE_RECEIPT_COMPLETE / ENVIRONMENT_BUILD_NOT_RUN`** — 協作者於別的環境完成。**論文 PDF 的 SHA-256 已由雲端 session 獨立複驗、逐字相符**；作者程式的兩個雜湊仍為轉錄。**不宣稱 G0 PASS**（環境未建置） |
| G1 輸入語義 | **`PASS`（`SEMANTIC_EQUIVALENCE_ESTABLISHED`）** — 依**一手全文**核對定理與證明。**先前的相反預測是錯的**，見下 |
| G2 group | 本研究側已填；`k` 的處置已裁決（見 6.4） |
| G4 輸出介面 | 本研究側已填；adapter contract 草案可用 |
| G3 狀態／重置／時間 | **`NOT_COMPARABLE` 的風險已由 G1 轉移到這裡**，見下 |
| G5–G8 | NOT RUN |

**雲端這側 2026-08-20 重新實測，封鎖狀態與 08-19 完全相同**（arXiv `000`、
`github.com` 與 `api.github.com` `403`、`raw.githubusercontent.com` `200`）。
G0 的解除**不是**雲端解封，是別的環境做的。

### G1 判定：紅旗解除，且**先前的預測是錯的**

劉老師 2026-08-20 取得 arXiv:2606.00419v4 全文並提供給雲端 session
（SHA-256 與協作者回報值相符）。核對結果與本文件先前的判斷**相反**：

先前（08-19 首次提出、08-20 上午重申）假設 POGO 的保證**需要**
`S_t = |Y_t − f(X_t)|` 的 residual 結構。**全文顯示不需要。**

- **Theorem 4.1 的假設只有三條**：`α∈(0,1)`、`S_t ≤ D t^q`、`T_j > 0`。
  沒有 `Y_t`、沒有預測器、沒有可交換性
  （論文明言 "We make no assumptions on the data stream"）。
- 被保證的量 `MisCov_T(j)` 只用到 `S_t`、`τ_t`、`c_t`。
- 逐步核對證明後，`S_t` 的結構只在 **Lemma B.2** 進入，而該處對 `S_t` 的
  全部要求是明寫的一句：**"since `S_t ≥ 0` because it's a non-conformity score"**。
- **作者自己就是這樣用的**：第 5.1 節 "we **directly generate non-conformity
  scores**"，式 (13)/(15)/(16) 完全沒有 `Y_t`、沒有預測器、沒有區間。

本研究這一側的兩個條件**已實測**（`scores_MD_2022_run1`，95 檔、5,240,974 點）：
`S_t ≥ 0` — min **0.4240**、負值 **0** 筆；`S_t ≤ D t^q` — max **23.8048**，
取 `q = 0`、`D = 23.81`。**兩條實質滿足。**

**錯在哪裡值得記住**：把論文「引入一個量的方式」讀成「定理對那個量的要求」。
前者是說明，後者才是假設，而兩者常寫在不同的節裡。
（08-20 上午那段「限定詞不能掉」的方法論意見**本身仍然成立**，
只是它據以推論的事實前提是錯的。gate 3.4 保留原文並標註，未刪除。）

**方向必須寫死**：POGO 的 `1{S_t > τ_t}`（miscover，目標比率 α）
＝本研究的 `exceed = 1`；POGO 的 `MisCov_T(j)` ＝本研究的 per-bin `|FAR − α|`。
另外論文的 adaptivity 指標「最長連續 `{S_t > τ_t}`」與 G5 規格已寫的
「最長 miscoverage streak」**是同一個量**，不需為對照新增或修改任何指標。

### 但風險只是轉移，不是消失

`RELATED_WORK_ONLY` 的風險沒了，**`NOT_COMPARABLE` 的風險移到 G3**。
POGO 的 wealth process `W_{t,j}` 是**乘法累積**的持久狀態；
本研究的狀態是 `W = 1440` 滾動視窗 + 18 點 exceed history + `frozen` 旗標。
兩者記憶形狀完全不同。**跨 case 攜帶與否、凍結期間是否跳過 `update`、
warm-up 如何對齊——三項都必須在跑之前寫死**，否則等於讓實作決定結論。

### 這件事對論文的意義比「多一個基線」大

R26 原本的目的是「檢查 POGO 能不能當基線」。G1 PASS 之後有更好的用途：
**POGO 是目前可得的、對 C2 最強的獨立檢驗。** 若一個演算法完全不同、
且帶有已證明保證的方法，在同一套 6-of-18 + Freeze-on-Alert 下呈現**相同的
凍結鎖死幾何**，那就直接證明該現象是**告警政策的性質，不是本方法的缺陷**。
**因此 G6 才是有價值的那一關，不是 G5。**

順帶：R25 之後**POGO 在 G5 上贏過本方法並不致命**——三項可守主張都不依賴
「本方法的校準器最好」。但 firewall 那一條仍全額有效直到 G8 回報。

**【2026-08-20 授權】Drive 的 R26 正本尚未反映 G1 `PASS` 與兩項裁決**，
正本現在寫的是「G1 未判、兩項契約待決」。gate 第 0 節原規定「分歧時以 Drive
正本為準」，但正本是舊的，所以該規則**暫時反轉**：在正本更新前以版控為準。
**劉老師已授權由後續排程輪代為更新正本**；更新後請移除 gate 第 0 節的暫時方框，
讓「哪一份權威」恢復只有一個答案。

**下一個可執行動作**：G3 狀態契約 → adapter contract → G5→G6→G7→G8。
**執行 owner：仍為 TBD。** G0 與 G1 都已完成，但**兩者都是閱讀與核對工作**；
從 G3 契約起需要的是**建環境與實作**，那才是真正需要指派的部分。

---

## 7. 下一步

### 本機（劉老師）

照 `docs/LOCAL_RUNBOOK.md`：

1. Phase 3.0 重跑 signal map（含 `--unit-override "active_power=p.u."`）
2. 六趟評分器（A/B/C × run1/run2）
3. `check_unit_consistency.py` → **預期 exit 0**
4. ~~回傳 Farm C 的 `out-of-range readings rejected` 數字~~
   **✅ 完成（2026-08-15，六趟 A/B/C × run1/run2 全部跑完）**

   | 風場 | 案數 | 總列數 | 拒絕合計 | 單通道最高 | vs C1 5% 門檻 |
   |---|---|---|---|---|---|
   | A | 22 | 1,196,747 | 0 | 0% | 通過 |
   | B | 15 | 859,065 | 4,488 | 0.47%（`sensor_52_avg`） | 通過 |
   | C | 58 | 3,187,136 | 143,700 | 2.13%（`sensor_194/195`） | 通過 |

   **三場都在 5% 門檻內，最差的 Farm C 為 2.13%**，與 G4 實測的
   p95 = 2.37% 一致——是預期範圍，不是異常。

   驗證過的三件事（不是看輸出文字，是逐欄比對 summary）：

   - **決定性**：三場的 `readings_rejected_per_column` run1 與 run2 **逐欄相等**，
     符合 C5 協定對 MD_2022 的 `bit_identical` 要求。
   - **加上 `--unit-override` 重跑後數字未變**，與先前已提交的 summary 完全相同。
     合理：override 改的是宣告單位標籤，不影響溫度／轉速通道的物理範圍過濾。
   - **Farm A 的 0 不是濾網沒作用**：`range_filter.enabled = true`、五個範圍全載入。
     「none」與「沒濾」在畫面上長得一樣，所以這一項是分開查證的（準則 3）。

   **新發現見 2.4：Farm B 的主軸承對也有故障碼，且失效型態與 Farm C 不同。**
5. ~~Phase 4：C0–C6 gate~~ **✅ 完成 2026-08-15，三場全部 PASS**（見第 1 節）

   gate 一定要加 `--farm`，並且每個風場跑一次。三場的 signal map 不同
   （Farm A 以 `not_available` 宣告無主軸承通道），一次 gate 只檢查一份 map。
   少了 `--farm`，三場的案子會全部拿去比對同一份 map，必然失敗——而且
   失敗方向會隨你指哪一份 map 而反轉，很容易誤判成「某個風場有問題」。

6. **Phase 5：`run_pipeline.py`** ← **現在在這裡**

7. **【2026-08-18 新增】用現在 main 上的 `evaluate_experiment.py` 重跑一次
   Phase 5 的評估步驟**（不必重跑 scorer 與校準層，逐案 CSV 已在版控裡）。
   指令與 2026-08-16 那次**完全相同**，包含 `--event-info-root`；
   尺規的改動不需要新參數。目的是讓 `experiments/MD_2022_a*_evaluation/`
   同時具備三數字誤報**與** lead time——雲端只能補前者（見 1.0）。
   跑完請確認 `evaluation.json` 有 `false_alarm_protocol` 欄位，
   且 `comparison.md` 表頭含 `frozen %` 與 `FAR frozen`。

8. **【2026-08-18 新增，2026-08-19 更新】下載全文與程式**
   （雲端全部封鎖，2026-08-19 已重新實測，見
   `docs/method/POGO_COMPATIBILITY_GATE.md` 第 3 節）：
   · ~~arXiv 2606.00419（6.7 的紅旗，最優先）~~ **✅ 全文已於 2026-08-18
     由另一 session 取得並完成四欄核對；但 repo 內無副本，且
     R26 的 source lock 仍需要它，所以下載這件事沒有取消**
   · ~~**【新】POGO 作者程式 `github.com/beepulbharti/pogo`**~~
     **✅ 2026-08-20 由協作者完成 source receipt**（見 6.8／gate 3.3）。
     **雲端不做這件事的原因是授權範圍，不是網路**，兩者不要混記。
   · arXiv 2606.20115（監看）
   · CARE 論文期刊版（6.3，步驟見 `CARE_PAPER_ACQUISITION.md` 第四節）
   · **【2026-08-20 新增，優先序僅次於 CARE】F10 那一組**——
     arXiv:2403.03868（JRSSB 2025 版更好）、2403.07728（CAP）、2503.16809。
     這是**引用缺口**，不是監看項：見 1.00 第二點。
     次要：2605.22004、2301.00584、1905.01059、2606.13780。

9. **【2026-08-19 新增】CARE v6 的被引用清單掃描。**
   引用索引 API（crossref／openalex／semanticscholar）雲端全部不可達，
   兩輪文獻掃描都卡在這一軸。**在它完成之前，稿件不得寫
   「first on CARE v6」或任何等價的資料集層新穎性主張**——
   沒掃過，就不知道有沒有人做過。

### 雲端

- 等 Phase 5 的真實數字，重新評估 6.1（目前 6.1 的結論只建立在合成資料上）
- ~~手稿英文段落（資料描述必須反映第 2 節的事實）~~
  **✅ 2026-08-16 交付兩節初稿**，見 `docs/manuscript/`：

  | 檔案 | 章節 | 狀態 |
  |---|---|---|
  | `01_dataset_and_preprocessing.md` | Dataset and preprocessing | 數字齊備，可審 |
  | `02_evaluation_protocol.md` | Evaluation protocol | **待 H 裁決**才能定稿 |

  第 2 節的七項資料事實全部寫進去了，包含最容易寫錯的一項：
  逐通道過濾在**取平均之前**，所以 Farm B 約 0.47% 的列是單通道值——
  **稿件不能寫成「一律取兩通道平均」**。

  Related Work 刻意未寫：等 6.3 與 **6.6** 的全文核對。
  用二手摘要寫 Related Work 是 R17 禁止的做法。

- ~~Results 等 6.5 的誤報率呈報方式裁決~~
  ~~**✅ 2026-08-17 R24 已裁決並實作，Results 與 Limitations 可以定稿**~~
  **✅ 2026-08-18 已交付初稿**：

  | 檔案 | 章節 | 狀態 |
  |---|---|---|
  | `docs/manuscript/03_results.md` | Results | 初稿，兩處 `[PENDING]`（H、Base Scorer 2） |
  | `docs/manuscript/04_limitations.md` | Limitations | 初稿 |

  誤報率欄位取自 1.0 的重算目錄，lead-time 欄位取自 2026-08-16 那輪
  ——**稿件表註必須寫明兩個來源**，不要讓讀者以為整張表出自同一次執行。
  `02_evaluation_protocol.md` 第 3 節也已補入 R24 三數字協定的英文敘述。

  Limitations 已含停滯剖面、α 不變性、有界凍結上限、六政策否證、
  單一 scorer、缺 CARE 基線、H 未定、Farm A 無主軸承、逐通道過濾在取平均之前、
  版本漂移 44/51 對 45/50、gate 定義未批准。

- **文獻掃描建議列為每次排程的固定項目**（依據見 `NOVELTY_WATCH` 第六節）：
  W1-ACAS 與這次的 2026-05 文獻都是偶然掃到的，沒有機制保證會掃到。
  沒有新發現也要記一筆，讓下次知道上次掃到哪裡。
  **✅ 2026-08-18 第一次執行**，記錄在
  `docs/literature/LITERATURE_SCAN_2026-08-18.md`，含已用過的關鍵詞與
  下一輪建議的三個新軸。**這次有新發現**（見 6.7）。
  **✅ 2026-08-19 第二次執行**，`LITERATURE_SCAN_2026-08-19.md`：
  無新紅旗；`regime-*` 語彙撞車累積到三筆，該規範升級為禁止項（見 6.7 末）。
  **三個軸只做到兩個**——CARE v6 被引用清單需引用索引 API，雲端不可達，
  已移入本機清單。**因此稿件不得寫「first on CARE v6」之類的話。**
  **✅ 2026-08-20 第三次執行**，`LITERATURE_SCAN_2026-08-20.md`：
  **本輪有新發現，而且是三次裡最重要的一次**——F10（selection-conditional
  coverage／FCR 控制是既有文獻，引用缺口，見 1.00 第二點）、
  F11（look-elsewhere effect，先例參照）、F12（工單作為弱標籤，佐證 4.3）。
  **被引用清單第三次做不到**（本輪重新實測五個端點全 `000`），
  「first on CARE v6」的禁令繼續有效。
  ⚠️ 本輪證據等級**低於前兩輪**：`arxiv.org` 不可達，連摘要原文都沒讀到，
  只讀到搜尋引擎的轉述。處理上要更保守。

- ~~abstract／introduction 的貢獻句凍結~~
  **✅ 2026-08-19 已解凍並交付初稿**（R25 裁決明文允許在 claim firewall
  下起草）：`docs/manuscript/00_contribution_statement.md`。
  三項可守主張、五條禁止寫法、四句 abstract／introduction 草稿，
  以及五項阻擋定稿的未決事項（H、Base Scorer 2、Phase 5 重跑、
  C0–C6 追認、Related Work）。**Related Work 仍凍結。**

- **【新】R26 POGO 相容性 gate 的 POGO 那一欄**（見 6.8）。
  本研究這一欄已填完，等本機取得全文與作者程式。
  在 G8 回報之前，稿件不得出現任何形式的「我們優於／不需要 POGO」，
  **也不得出現「POGO 不適用於本問題」**。

---

## 8. 文件位置

| 內容 | 在哪 |
|---|---|
| **入口／三十秒導覽** | `README.md`（repo 根目錄，2026-08-17 新增） |
| 本機執行手冊 | `docs/LOCAL_RUNBOOK.md`（v1.1） |
| 本文件 | `docs/PROJECT_STATUS.md` ← **狀態以這裡為準** |
| 凍結鎖死的量測與候選政策否證 | `docs/FREEZE_LOCKIN_FINDINGS.md`（2026-08-17） |
| 開發日誌 | Google Drive，`風能運維論文協作開發日誌 v*`（逐次記錄「做了什麼」） |
| 研究方向與方法論筆記 | Google Drive，v4（待確認是否正式取代 v3） |
| **手稿英文草稿** | `docs/manuscript/`（2026-08-16 新增，先讀該目錄的 README；2026-08-18 補 Results 與 Limitations；2026-08-19 補貢獻句與界線四） |
| **貢獻定位與 claim firewall** | `docs/manuscript/00_contribution_statement.md` + 該目錄 README 界線四（2026-08-19，依 R25 裁決） |
| **POGO 相容性 gate** | `docs/method/POGO_COMPATIBILITY_GATE.md`（2026-08-19 進版控；規格正本在 Drive R26） |
| **文獻取得與新穎性監看** | `docs/literature/`（2026-08-16 新增；2026-08-18 起每次排程記錄一份掃描） |
| **R24 三數字協定對既有輸出的重算** | `experiments/three_number_recheck_2026-08-18/`（2026-08-18，見 1.0） |
| **告警選擇效應的代數下界＋前提稽核** | `experiments/alarm_selection_floor_2026-08-20/`（2026-08-20，見 1.00；量測併入 `FREEZE_LOCKIN_FINDINGS` 2.3a） |
| **給 Drive 這側的入口說明** | Google Drive，`【必讀】GitHub 專案入口與雙軌分工 v1.0`（2026-08-17） |
| 程式 | `scripts/`，34 支（實數；先前記的 26／30／32 已過期） |

### 8.0 為什麼 2026-08-17 補了一個 README（起因見下）

劉老師指出**其他協作者可能不知道有這個 repo**。查證後成因有三層，
權限不是其中之一——repo 是 public，任何人拿到連結就能讀：

1. **repo 根目錄沒有 README。** 點進來看到的是二十幾個輸出資料夾的清單，
   沒有一行字說明這是什麼、該從哪讀起、哪一份才算數。入口是空的，
   所以「有連結」等於「沒有連結」。
2. Drive 索引雖已寫「現況以 GitHub 為準」，但那份索引 2026-08-17 17:48
   才建立，且連結埋在長文中間。
3. **最關鍵的一層：`main` 落後兩天。** 見下方 8.0a。

已補：repo 根目錄 `README.md`（刻意不寫任何會變動的數字，只寫不會變的部分：
從哪讀起、怎麼跑、目錄結構、雙軌分工、分支與 PR 規矩），
以及 Drive 上的《【必讀】GitHub 專案入口與雙軌分工 v1.0》與索引 v1.4。

### 8.0a `main` 曾落後兩天 — 2026-08-17 已解除，但規則留著

**已解除**：劉老師 2026-08-17 裁示合併，PR #13 與 #14 均已併入 `main`，
本文件與程式的最新狀態現在真的在 `main` 上。

留下這一節是因為**問題本身會重複發生**。當時的情況是：本文件開頭寫著
「請從 `main` 取用」，而 `main` 上的 PROJECT_STATUS 停在 2026-08-16
（自我測試記為 10 支、291 checks），兩天的成果全躺在未合併的 draft PR 裡。
照 `main` 讀會讀到舊狀態，**而且沒有任何跡象提示那是舊的**——
第 5 節那張清單的典型模式：不會報錯，只是內容是錯的。

**由此形成的規則**：

- 把現況放進版控，只有在東西**真的併進 `main`** 之後才成立。
  開著的 draft PR 不算數，它對讀 `main` 的人是不存在的。
- 一輪工作結束就把 PR 推到可合併狀態，不要讓兩份以上的 PR 疊著等——
  疊著等除了讓 `main` 失效，還會製造 6.6 那種小節撞號。
- 合併後**一定重跑自我測試**，不要假設兩份 PR 互不干擾。
  本次合併後實跑：12 支、370 checks、0 failed。

**溝通與文件語言**：討論與進度文件用中文，實際投稿文件用英文。

---

### 8.1 文件命名規範（2026-08-15 新增，起因見下）

**問題**：多位協作者（Gemini Spark、Codex B、排程自動化研究助理，加上不只一個
同時執行的 session）各自遞增版本號，於是同一個版本號被不同人用在不同文件上。
截至 2026-08-15 已發生 **6 組撞號**：v1.7、v1.8、v2.2、v2.4 各兩份，
v3.0 三份，v3.9 兩份。最嚴重的一次是 v4.0 已存在後又出現一份 v3.9，
**最新的紀錄反而掛著最舊的版號**。

**根因**：版本號是一個需要全域協調的計數器。並行寫入時它必然碰撞——
這不是誰粗心，是編號方式本身不適用於多寫入者。

**規範**：日誌與進度文件標題一律採

```
<文件類型> v<版號> — <YYYY-MM-DD HHMM> — <作者>
例：風能運維論文協作開發日誌 v4.1 — 2026-08-15 2211 — 排程自動化研究助理
```

- **時間戳是真正的識別碼**，版號只是給人讀的順序提示。
  時間戳天然唯一，不需要任何人協調。
- 撞號時**不改動歷史文件的版號**（其他文件會引用它），
  只補上時間戳與作者以資區別。
- 新建文件前**先看資料夾裡最大的版號**再遞增；不確定就直接用時間戳。

**判斷「哪一份最新」時，一律看時間戳，不要看版號。**

> 更根本的解法是別再用 Drive 追狀態：**現況以本文件為準，日誌只記流水帳**。
> 本文件在版控裡，天然有唯一的歷史與衝突處理，不會有這個問題。

---

*最後更新：2026-08-20（Asia/Taipei，當日第二次：G1 全文核對與兩項裁決）。狀態有變動時請更新本文件，不要只寫在對話裡。*
